rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /songs/{allPaths=**} {
      // Allow read and write access to all files under the 'songs/' directory
      // for any authenticated user.
      allow read, write: if request.auth != null;
    }
  }
}