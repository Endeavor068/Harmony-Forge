{
  "entities": {
    "Song": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Song",
      "type": "object",
      "description": "Represents a musical song or hymn, including its title, author, year of creation, and lyrics for its verses and chorus.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Song entity, must match the document ID."
        },
        "number": {
          "type": "string",
          "description": "The identifying number assigned to the song (e.g., '001')."
        },
        "title": {
          "type": "string",
          "description": "The primary title of the song."
        },
        "author": {
          "type": "string",
          "description": "The creator or writer of the song."
        },
        "year": {
          "type": "string",
          "description": "The year the song was composed or published."
        },
        "verses": {
          "type": "array",
          "description": "An ordered list of verses for the song.",
          "items": {
            "type": "string"
          }
        },
        "chorus": {
          "type": "string",
          "description": "The lyrics for the song's chorus, if applicable."
        },
        "partitionUrl": {
          "type": "string",
          "description": "URL or Data URI for the sheet music image."
        },
        "audioUrl": {
          "type": "string",
          "description": "URL or Data URI for the audio recording."
        }
      },
      "required": [
        "id",
        "number",
        "title",
        "author",
        "verses"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/songs/{songId}": {
      "schema": {
        "$ref": "#/backend/entities/Song"
      },
      "description": "Collection of all musical songs or hymns. Document ID {songId} must match the 'id' field in the document data."
    }
  }
}
